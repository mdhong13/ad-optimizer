# Railway OOM 회피 — wheel 우선 (source build 차단)
--prefer-binary

# Web (PWA)
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
jinja2>=3.1.0
python-multipart>=0.0.12

# Scheduler
apscheduler>=3.10.0

# LLM
anthropic>=0.40.0
openai>=1.0.0

# Ad Platforms
facebook-business>=20.0.0
google-ads>=28.0.0
tweepy>=4.14.0

# gRPC transitive (google-ads 의존) — wheel 보장 버전 명시
grpcio>=1.60.0,<2.0.0
protobuf>=4.25.0,<6.0.0

# HTTP — knowin_crawler 가 sync requests 사용
requests>=2.31.0

# HTML 파싱 — knowin_body_fetcher (네이버 지식인 질문 본문 추출)
beautifulsoup4>=4.12.0

# HTTP / Data
httpx>=0.27.0
feedparser>=6.0.0

# Image processing (multi-platform resize)
Pillow>=10.0.0

# Config / Utils
python-dotenv>=1.0.0
rich>=13.0.0

# DB
pymongo>=4.9.0

# Auth
google-auth>=2.36.0
google-auth-oauthlib>=1.3.0
